# 本轮明确不处理的“假实现”问题

本文件只做 scope guard。

**不提供整改方案。**

后续将单独建立“业务动作结果契约”专题。

---

## F-01 页面跳转

旧系统会向后端返回类似：

```json
{
  "businessType": "page_jump",
  "permissionCode": "punch-card-records"
}
```

后端据此真正执行页面动作。

当前 Agent 的 page_jump / 自然语言“已打开”不等于该业务契约已经实现。

本轮：

**不改。**

---

## F-02 人工转接

当前“已为您转接人工”没有对应真实后端业务动作。

本轮：

**不改。**

---

## F-03 Leave 最终业务单据公共结果

当前内部可生成 Leave Form / dry-run form，但顶层公共结果与后端消费契约尚未形成最终真实业务动作闭环。

本轮：

**不改最终业务动作契约。**

WP-02 只保证最终边界之前的 Leave Form 业务事实正确。

---

## F-04 撤回 / 销假等后端动作

当前相关页面/动作仍属于后续 business action contract。

本轮：

**不改。**

---

## F-05 任何类似问题

判断标准：

> Agent 声称“已经执行某业务动作”，但实际上缺少后端可执行的稳定 action JSON / command。

全部留到后续专题。

实施 AI 不得借本工程包顺手处理。
